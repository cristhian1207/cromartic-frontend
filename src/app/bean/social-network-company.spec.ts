import { SocialNetworkCompany } from './social-network-company';

describe('SocialNetworkCompany', () => {
  it('should create an instance', () => {
    expect(new SocialNetworkCompany()).toBeTruthy();
  });
});
